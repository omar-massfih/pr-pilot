# Repository where feature branches and pull requests will be created.
repo = "/absolute/path/to/your/repository"
state_dir = "~/.pr-pilot"

[implementer]
name = "codex" # "codex" or "cursor"
# model = "optional-model-name"
limit_poll_seconds = 60
limit_max_wait_seconds = 0 # 0 waits indefinitely; Ctrl-C still stops immediately.

[reviewer]
name = "cursor" # A different provider is optional, but gives stronger independence.
# model = "optional-model-name"
limit_poll_seconds = 60
limit_max_wait_seconds = 0

[github]
base_branch = "main"
draft = true

[babysit]
enabled = true
interval_seconds = 30
max_cycles = 60
max_fix_attempts = 3
require_approval = false

[memory]
enabled = true
database = "~/.pr-pilot/memory.db"
embedding_model = "BAAI/bge-small-en-v1.5"
profile_provider = "implementer"
max_file_bytes = 524288
chunk_chars = 1600
chunk_overlap = 200
context_results = 10
context_chars = 12000
relationship_depth = 1
relationship_threshold = 0.80

[telegram]
token_env = "TELEGRAM_BOT_TOKEN"
allowed_chat_ids = [123456789]
